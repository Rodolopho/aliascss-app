import { Callout } from 'nextra/components'
import SearchStaticClassNames from '../../app/components/staticClassNames';
import { staticClassNames } from '../../node_modules/aliascss/lib/index';



## Static ClassNames
<SearchStaticClassNames/>
<br/>
{/* Give Below is the List of Static ClassNames that you can use to style your webpage.
<table style={{'height':'700px','overflow-y':'scroll','overflow':'hidden'}}>
  <tr>
    <th>ClassName</th>
    <th>Statement</th>  
  </tr>
  {Object.keys(staticClassNames).map((item, index) => (
    <tr key={index}>
      <td>{item}</td>
      <td>{staticClassNames[item]}</td>
    </tr>
  ))}
 
</table> */}

  

