# Button
import {DemoButton} from '../../../acss/demo-button';
import content from '!!raw-loader!../../../acss/demo-button';
import component from '!!raw-loader!../../../acss/components/button'
import { compileMdx } from 'nextra/compile'
import { MDXRemote } from 'nextra/mdx-remote'
import { Tabs } from 'nextra/components'
import './button.mdx.css';

export const filenames=['Demo.tsx','components/button.tsx'];

export const Wrapper=()=>{
    
return(
    <div className="w-100p mt-32px flex-center mh-200px b1px-s-grayA6">
        <DemoButton/>
    </div>
)
}

<Wrapper/>
<Tabs items={[filenames[0], filenames[1]]}>
  <Tabs.Tab>
        <MDXRemote className="_pre-white-space-pre-wrap"
            compiledSource={await compileMdx('```jsx  showLineNumbers copy filename="'+filenames[0]+'" '+ '\n'+content+'\n'+'```')}
            components={{ MyComponent: () => <div>My Component</div> }}
            scope={{ myVariable: content }}
        />
  </Tabs.Tab>
  <Tabs.Tab>
        <MDXRemote className="_pre-white-space-pre-wrap"
            compiledSource={await compileMdx('```jsx  showLineNumbers copy filename="'+filenames[1]+'" '+ '\n'+component+'\n'+'```')}
            components={{ MyComponent: () => <div>My Component</div> }}
            scope={{ myVariable: component }}
        />
  </Tabs.Tab>
  {/* <Tabs.Tab></Tabs.Tab> */}
</Tabs>
